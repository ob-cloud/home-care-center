import { getAction, postAction, putAction, deleteAction, deleteJsonAction, httpAction, downloadAction } from './action'
import { axios } from './request'

export {
  axios,
  getAction,
  postAction,
  putAction,
  deleteAction,
  deleteJsonAction,
  httpAction,
  downloadAction
}
